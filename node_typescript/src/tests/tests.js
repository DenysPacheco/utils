import assert from "assert/strict"

describe("Basic addition test", () => {
    it("should add up to 3", () => {
        assert.equal(2 + 1, 3)
    })

    it("should equal 8", () => {
        assert.equal(4 * 2, 8)
    })
})
